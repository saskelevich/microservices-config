server:
  port: 8761

spring:
  cloud:
    config:
      uri: http://192.168.0.134

eureka:
  instance:
    hostname: localhost
  # standalone mode
  client:
    registerWithEureka: false
    fetchRegistry: false
    # serviceUrl:
      # defaultZone: http://localhost:8761/eureka/
      # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/